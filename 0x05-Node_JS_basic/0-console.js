/**
 * Funtion to display a message on the STDOUT.
 * @param {String} message Message to display.
 */

const displayMessage = (message) => {
	console.log(message);
};

module.exports = displayMessage;
